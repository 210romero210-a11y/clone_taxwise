# This environment variable is used in Production and Preview environments to deploy your Convex project
# This variable is not needed in development.
# You can create a Production or Preview deploy key at https://dashboard.convex.dev/project/settings#production-deploy-keys
CONVEX_DEPLOY_KEY=

# WorkOS Configuration (IRS Publication 1345 compliant authentication)
# Get your credentials from https://workos.com/
WORKOS_API_KEY=your_workos_api_key
WORKOS_CLIENT_ID=your_workos_client_id

# Encryption Key for session tokens and PII (required for production)
# Generate a secure 32-byte key: openssl rand -hex 32
ENCRYPTION_KEY=your_32_byte_encryption_key_here

# MeF (Modernized e-File) Configuration - IRS Publication 4163 / 4164
# Set to true for test environment, false for production
MEF_IS_TEST_MODE=true

# IRS MeF Gateway URL
# Production: https://efile.irs.gov/MEF/1040/PCK/1040
# Test: https://mefmt-efile.web.irs.gov/MEF/1040/PCK/1040
MEF_TRANSMISSION_URL=https://mefmt-efile.web.irs.gov/MEF/1040/PCK/1040

# SOAP Action header for IRS submissions
MEF_SOAP_ACTION=http://www.irs.gov/efile/Submit

# Request timeout in milliseconds (default: 30000)
MEF_TIMEOUT=30000

# Maximum retry attempts for failed submissions (default: 3)
MEF_MAX_RETRIES=3
